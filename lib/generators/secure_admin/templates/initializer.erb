# SecureAdmin config file. Generated on <%= DateTime.now.to_s(:long) %>
# See github.com/harssh/secure_admin for more informations

SecureAdmin.config do |config|


  ################  Global configuration  ################

  # Set the admin name here (optional second array element will appear in red). For example:
  config.main_app_name = ['<%= Rails.application.engine_name.titleize.chomp(' Application') %>', 'Admin']
  # or for a more dynamic name:
  # config.main_app_name = Proc.new { |controller| [Rails.application.engine_name.titleize, controller.params['action'].titleize] }

  # SecureAdmin may need a way to know who the current user is]
  config.current_user_method { current_<%= @model_name.underscore %> } # auto-generated

  
  
end
